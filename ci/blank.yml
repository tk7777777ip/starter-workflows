name: ROOT>

on: [pull]

jobs:-Getvar-user.exe

root.exe
  build:OCL27.76-69-6-3

    runs-on: Android.os-Apps

    steps:
    01- uses: actions/ adb shell/adb devices/adb kill-server/ adb-root
  02  - name: Run a stack-overflow script
      run: echo then+ping
    03- name: Run an array of script
      run:echo  ping|
        echo
        echo, and deploy .

